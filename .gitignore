# ── macOS system files ───────────────────────────────
.DS_Store
._*
.AppleDouble
.LSOverride
.Spotlight-V100
.Trashes

# ── Editor / IDE ────────────────────────────────────
.vscode/
.idea/
.zed/
.cursor/
*.swp
*.swo
*~

# ── Logs / temp / backup ────────────────────────────
*.log
*.tmp
*.bak
*.orig

# ── Environment / secrets ───────────────────────────
.env
.env.local
.env.*.local

# ── Node / JS build artifacts (if ever added) ───────
node_modules/
dist/
build/
.cache/

# ── Python artifacts (if any local scripts) ─────────
__pycache__/
*.pyc
*.pyo
.venv/
venv/

# ── Claude Code working artifacts ───────────────────
.claude/
